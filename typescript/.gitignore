node_modules/
yarn.lock
.yarn
.npmrc
dist/
*.mmdb

# The root .gitignore ignores every path named `launch` (local build output).
# These are the launch_options() golden fixtures, not build output.
!/tests/fixtures/launch/
