cloverlabs-camoufox
# 1.61 sends viewport.isMobile in Browser.setDefaultViewport, which this
# juggler's protocol schema rejects -- every context creation fails. Matches the
# cap in python/pyproject.toml.
playwright<1.61
